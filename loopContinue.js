var listaDeNomes = ['Cesar', 'Pamela', 'Camila', 'Hendy']
for(var i=0; i < listaDeNomes.length; i++) {
    if(listaDeNomes[i] == 'Cesar') {
        continue

        // Qdo o nome for Cesar, ele simplesmente passa para o próximo
    }
    console.log(listaDeNomes[i])
}